apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: xenonstack-service
  description: |
    A service for testing Backstage functionality. Configured for
    GitHub Actions.
  annotations:
   'backstage.io/kubernetes-id': @xenon
spec:
  type: service
  owner: CloudOps
  lifecycle: production
